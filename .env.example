# Example environment variables (GPU, cache paths, APIs)

# OpenAI API key — required for the vision image-interpretation pipeline
# (vision/pipeline.py, used by ingest_documents.py --vision)
OPENAI_API_KEY=your_openai_api_key_here